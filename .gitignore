/.vscode
/dist
/node_modules
.idea
CLAUDE.md

# Local-only: planning docs, tooling config, and backups (not versioned)
.claude/
/docs
/files
/files.zip

# Local-only: Playwright / preview proof screenshots (not versioned)
/.playwright-mcp/
/*.jpeg

# Local-only: official Niimbot app installers + Android APK (reference material for reverse-engineering
# the cloud API — large PROPRIETARY binaries, NEVER commit to this public repo)
/Niimbot official download material/
.superpowers/

# Generated assets (OpenMoji stickers copied from node_modules)
/public/stickers/openmoji
